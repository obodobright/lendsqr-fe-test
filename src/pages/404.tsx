import { Layout } from "@/layout";
import RedirectView from "@/views/ErrorView";

export default function Error() {
    return (
        <Layout>
            <RedirectView />
        </Layout>
    )
}